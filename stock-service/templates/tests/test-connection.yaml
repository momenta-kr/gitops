apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stock-service.fullname" . }}-test-connection"
  labels:
    {{- include "stock-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stock-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
